OPENAI_API_KEY=sk-your-api-key
# Paste only the raw Replicate token (e.g. r8-...). We will strip accidental "Bearer"/"Authorization" prefixes if present.
REPLICATE_API_TOKEN=r8-your-api-token
PORT=3000
